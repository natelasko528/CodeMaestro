# Testing & QA — Wrapper

Golden flows:
1) Start session → planner message appears
2) Propose 2-file edit → diff preview → apply → files updated
3) Run tests via allowlist → output piped back → coach blocks until green
4) Store key → restart → key persists; no logs contain secrets
