# AI Build Prompt — Use with Claude Code (Opus 4.5)

Build the CodeMaestro wrapper. Follow /spec exactly.

Deliver:
- /extension (TypeScript)
- /server (Node or Python)

Requirements:
- Provider-agnostic product
- SecretStorage key handling
- Planner/Player/Coach orchestration mode
- Multi-file propose-edit with diff preview/apply
- Tool allowlist runner

Commands:
- npm install
- npm run build
- npm test
