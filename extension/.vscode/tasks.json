{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build server + extension",
      "type": "shell",
      "command": "cd \"${workspaceFolder}/../server\" && npm run build && cd \"${workspaceFolder}\" && npm run build",
      "problemMatcher": ["$tsc"],
      "group": "build"
    }
  ]
}
